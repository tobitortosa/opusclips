# Copia este archivo como .env y pone tu API key.
# IMPORTANTE: usa la cuenta que quieras que pague estos analisis,
# NO la cuenta del trabajo. La app lee SOLO esta variable, nunca
# una ANTHROPIC_API_KEY que tengas en el sistema.
CLIPS_API_KEY=sk-ant-...
